
// Application routes
export const ROUTES = {
  AUTH_LOGIN: "/auth/login",
  DASHBOARD: "/dashboard",
  HOME: "/",
};

// Keys used for browser storage (cookies, localStorage, etc.)
export const STORAGE_KEYS = {
  COOKIE_ACCESS_TOKEN: "demo-access-token",
  COOKIE_REFRESH_TOKEN: "demo-refresh-token",
};